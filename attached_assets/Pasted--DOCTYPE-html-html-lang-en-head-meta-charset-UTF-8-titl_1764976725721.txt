<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>IFCDC • Radio | Barbershop | Community</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <!-- Make sure this path points to your CSS file -->
  <link rel="stylesheet" href="/styles.css" />
  <style>
    /* ====== TEMP STYLES – you can move these into styles.css ====== */

    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: radial-gradient(circle at top, #111 0, #000 55%, #050505 100%);
      color: #f5f5f5;
    }

    .ifcdc-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 20px;
      background: #000; /* Black bar */
      border-bottom: 1px solid #262626;
      position: sticky;
      top: 0;
      z-index: 20;
    }

    .ifcdc-logo-wrap {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .ifcdc-logo {
      height: 46px;
      width: auto;
      object-fit: contain;
    }

    .ifcdc-brand-text {
      font-weight: 800;
      letter-spacing: 0.12em;
      font-size: 0.85rem;
      text-transform: uppercase;
      color: #d4af37;
      text-shadow: 0 0 4px rgba(212,175,55,0.7);
    }

    .ifcdc-nav-actions {
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .ifcdc-nav-link {
      text-decoration: none;
      font-weight: 700;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      font-size: 0.8rem;
      color: #d4af37;
      text-shadow: 0 0 3px rgba(212,175,55,0.65);
      transition: transform 0.15s ease, text-shadow 0.15s ease, opacity 0.15s ease;
    }

    .ifcdc-nav-link:hover {
      transform: translateY(-1px);
      text-shadow: 0 0 7px rgba(212,175,55,1);
      opacity: 0.95;
    }

    .ifcdc-nav-primary {
      padding: 6px 14px;
      border-radius: 999px;
      border: 1px solid #d4af37;
      box-shadow: 0 0 6px rgba(212,175,55,0.6);
      background: radial-gradient(circle at top, #2b210b, #080808 70%);
    }

    .ifcdc-main {
      max-width: 1080px;
      margin: 0 auto;
      padding: 28px 16px 40px;
    }

    .ifcdc-hero {
      display: flex;
      flex-direction: column;
      gap: 18px;
      margin-bottom: 32px;
    }

    .ifcdc-hero-title {
      font-size: 1.9rem;
      line-height: 1.2;
      font-weight: 800;
    }

    .ifcdc-hero-title span {
      color: #d4af37;
      text-shadow: 0 0 6px rgba(212,175,55,0.7);
    }

    .ifcdc-hero-sub {
